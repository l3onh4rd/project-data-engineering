version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11

  build:
    commands:
      - echo "Building Lambda package..."
      - zip -r function.zip main.py

artifacts:
  files:
    - function.zip